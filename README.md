# my-fastfetch-config

# ensure you have fastfetch and git installed then run 

```bash
fastfetch --gen-config

# then do

git clone https://github.com/Christine-oss-web/my-fastfetch-config.git

# then do

rm -rf ~/.config/fastfetch/config.jsonc

# then copy it

cp ~/my-fastfetch-config/config.jsonc ~/.config/fastfetch
